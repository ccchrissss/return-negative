function makeNegative(num) {

  return num < 0 ? num : num * -1
  
}


// params: num
// return: num    // make it negative if it is not already negative
// e.g. makeNegative(458)
// results in -458

// if the number is less than 0 return the number
// else return the number * -1